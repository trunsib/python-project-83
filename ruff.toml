line-length = 100
target-version = "py310"

exclude = [
    ".git",
    "__pycache__",
    ".venv",
    "venv",
    "build",
    "dist",
    "*.egg-info",
]

lint.select = [
    "E",
    "W",
    "F",
    "I",
]

lint.ignore = [
    "E501",
]

[lint.isort]
known-first-party = ["page_analyzer"]
